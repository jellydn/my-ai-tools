---
"my-ai-tools": patch
---

Add first-class Pool CLI support: agent guidelines (AGENTS.md with sandbox configuration), install, config copy, generate, backup, banner, BATS tests (15), and docs (README, MCP table, built-in skills).

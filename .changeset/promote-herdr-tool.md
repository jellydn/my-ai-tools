---
"my-ai-tools": patch
---

Promote herdr to a first-class supported tool. Ship AGENTS.md guidelines (matching the Conductor pattern), add install_herdr() and copy_herdr_configs() to the installer, and document in README. The agent skill itself is installed from upstream (herdr.dev) — no forked SKILL.md.
